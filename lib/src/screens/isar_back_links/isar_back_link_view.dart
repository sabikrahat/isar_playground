import 'package:flutter/material.dart';

class IsarBackLinkView extends StatelessWidget {
  const IsarBackLinkView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Isar Back Link'),
      ),
      body: const Center(
        child: Text('Hello World'),
      ),
    );
  }
}
